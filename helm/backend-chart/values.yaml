replicaCount: 1

image:
  repository: myregistry/backend
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8000

ingress:
  enabled: false
  host: backend.local
  annotations: {}
